{
  "name": "canvas-browser-extension",
  "version": "2.8.6",
  "description": "Canvas Browser Extension for seamless tab synchronization",
  "type": "module",
  "scripts": {
    "build": "cross-env NODE_ENV='production' node build.mjs",
    "build:dev": "cross-env NODE_ENV='dev' node build.mjs",
    "clean": "rimraf build packages",
    "lint": "eslint src/ --ext .js,.mjs",
    "lint:fix": "eslint src/ --ext .js,.mjs --fix",
    "smoke": "node scripts/smoke.mjs"
  },
  "keywords": [
    "browser-extension",
    "tabs",
    "synchronization",
    "canvas"
  ],
  "author": "me@idnc.sk",
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "archiver": "^7.0.1",
    "cross-env": "^10.1.0",
    "esbuild": "^0.28.1",
    "eslint": "^9.39.4",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-promise": "^7.2.1",
    "fs-extra": "^11.3.4",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "iconify-icon": "^3.0.2",
    "socket.io-client": "^4.8.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
